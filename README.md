IF ASSIGNED_LABEL label_guid IN folderA,folderB THEN RESOLVE_ISSUE
IF ASSIGNED_LABEL label_guid IN folderA,folderC THEN MOVE_ISSUE_TO folder_guid
IF ASSIGNED_LABEL label_guid IN folderA THEN COMMENT comment_text
IF ASSIGNED_LABEL label_guid IN folderA THEN ASSIGN_LABEL label_guid
IF ASSIGNED_LABEL label_guid IN folderA THEN ASSIGN_TAG tag_text
IF ISSUE_CREATED IN folderB THEN RESOLVE_ISSUE
IF ISSUE_CREATED IN folderB THEN MOVE_ISSUE_TO folder_guid
IF ISSUE_CREATED IN folderB THEN COMMENT comment_text
IF ISSUE_CREATED IN folderB THEN ASSIGN_LABEL label_guid
IF ISSUE_CREATED IN folderB THEN ASSIGN_TAG tag_text
IF ISSUE_MOVED TO folderC THEN RESOLVE_ISSUE
IF ISSUE_MOVED TO folderC THEN MOVE_ISSUE_TO folder_guid
IF ISSUE_MOVED TO folderC THEN COMMENT comment_text
IF ISSUE_MOVED TO folderC THEN ASSIGN_LABEL label_guid
IF ISSUE_MOVED TO folderC THEN ASSIGN_TAG tag_text
IF ISSUE_RESOLVED IN folderA THEN RESOLVE_ISSUE
IF ISSUE_RESOLVED IN folderA THEN MOVE_ISSUE_TO folder_guid
IF ISSUE_RESOLVED IN folderA THEN COMMENT comment_text
IF ISSUE_RESOLVED IN folderA THEN ASSIGN_LABEL label_guid
IF ISSUE_RESOLVED IN folderA THEN ASSIGN_TAG tag_text
IF COMMENTED_ON_BY user IN folderB THEN RESOLVE_ISSUE
IF COMMENTED_ON_BY user IN folderB THEN MOVE_ISSUE_TO folder_guid
IF COMMENTED_ON_BY user IN folderB THEN COMMENT comment_text
IF COMMENTED_ON_BY user IN folderB THEN ASSIGN_LABEL label_guid
IF COMMENTED_ON_BY user IN folderB THEN ASSIGN_TAG tag_text
